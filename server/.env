

# MongoDB Configuration
MONGODB_URI="mongodb+srv://<username>:<password>@cluster0.abc123.mongodb.net/yourdbname?retryWrites=true&w=majority"

# JWT Authentication
JWT_SECRET=d34f28e52cc4626381ecebce08fc8c1cf25a15a85ce3ea9cb3f35678cb1d0d94 # Generate via: `openssl rand -hex 32`

# Server Settings
PORT=5000  # Optional (Render will override this)
NODE_ENV="development"  # Change to "production" when deploying

# Email Service (If using Nodemailer)
EMAIL_USER="your@gmail.com"
EMAIL_PASS="your_app_password"  # Use Gmail App Password if 2FA enabled

# Frontend URL (For CORS)
CLIENT_URL="http://localhost:3000"  # Replace with Vercel URL in production

# Optional: Rate Limiting (requests per window)
RATE_LIMIT_WINDOW=15  # minutes
RATE_LIMIT_MAX=100    # requests per window